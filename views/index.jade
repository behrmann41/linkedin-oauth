extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
    p
      |Hello #{user.displayName}
      ||
      a(href="/logout") Logout
  else
    p
      a(href="/auth/linkedin") Login
